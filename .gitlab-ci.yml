stages:
  - build
  - test
  - start

cache:
  paths:
    - back-end/node_modules/

build:
  stage: build
  script:
    - docker build -t theme-base .
    # PROD DATABASE: - docker run -d -p 4000:4000 --name theme-base -e MONGODB_URI=$MONGODB_URI theme-base
    - docker run -d -p 4000:4000 --name theme-base -e MONGODB_TEST_URI=$MONGODB_URI theme-base
  artifacts:
    paths:
      - back-end/node_modules/

test:
  stage: test
  script:
    - echo "test"

start:
  stage: start
  script:
    - echo "start"
