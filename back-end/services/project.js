/**
 * Service that calculates the new prices for projects based on the amount of customer that are commited to buy the project.
 */

/**
 * Returns information about the project object based on project id.
 * @param {string} projectId
 */
const getProjectDetails = projectId => {
  console.log('get details');
};

/**
 * Save the updated project object to the database. Possibly perform additional actions.
 * @param {Project} projectData
 */
const updateProjectDetails = projectData => {
  console.log('update details');
};

/**
 * Calculate the updated project information
 */
const calculateDiscount = projectId => {
  console.log('discount');
};
